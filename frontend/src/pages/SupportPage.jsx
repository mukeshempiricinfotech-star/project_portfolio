import {useEffect,useState} from 'react';import {TicketList} from '../components/TicketList.jsx';export default function SupportPage(){const [tickets,setTickets]=useState([]);const [selected,setSelected]=useState(null);useEffect(()=>{fetch(`${import.meta.env.VITE_API_URL}/tickets`,{headers:{Authorization:`Bearer ${localStorage.getItem('accessToken')}`}}).then(r=>r.json()).then(x=>setTickets(x.data));},[]);return <><h1>Support</h1><TicketList tickets={tickets} onSelect={setSelected}/>{selected&&<aside><h2>{selected.number}: {selected.subject}</h2><p>{selected.description}</p></aside>}</>}