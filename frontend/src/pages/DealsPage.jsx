import {useEffect,useState} from 'react';import {DealPipelineBoard} from '../components/DealPipelineBoard.jsx';export default function DealsPage(){const [deals,setDeals]=useState([]);useEffect(()=>{fetch(`${import.meta.env.VITE_API_URL}/deals`,{headers:{Authorization:`Bearer ${localStorage.getItem('accessToken')}`}}).then(r=>r.json()).then(x=>setDeals(x.data));},[]);async function move(id,stage){const before=deals;setDeals(x=>x.map(d=>d.id===id?{...d,stage}:d));const r=await fetch(`${import.meta.env.VITE_API_URL}/deals/${id}`,{method:'PATCH',headers:{'Content-Type':'application/json',Authorization:`Bearer ${localStorage.getItem('accessToken')}`},body:JSON.stringify({stage})});if(!r.ok)setDeals(before);}return <><h1>Deals</h1><DealPipelineBoard deals={deals} onMove={move}/></>}