export function ContactCard({contact,onEdit}){const name=`${contact.firstName} ${contact.lastName}`;return <article className="card" aria-labelledby={`contact-${contact.id}`}><header><h3 id={`contact-${contact.id}`}>{name}</h3>{contact.title&&<span>{contact.title}</span>}</header><a href={`mailto:${contact.email}`}>{contact.email}</a>{contact.phone&&<a href={`tel:${contact.phone}`}>{contact.phone}</a>}<ul aria-label="Tags">{contact.tags?.map(tag=><li key={tag}>{tag}</li>)}</ul><button type="button" onClick={()=>onEdit(contact)}>Edit {name}</button></article>}