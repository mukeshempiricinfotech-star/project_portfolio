import request from 'supertest';jest.unstable_mockModule('../../src/middleware/authMiddleware.js',()=>({authenticate:(req,_r,n)=>{req.user={sub:'user-1',role:'sales'};n();},authorize:()=> (_q,_r,n)=>n()}));jest.unstable_mockModule('../../src/services/contactService.js',()=>({listContacts:jest.fn().mockResolvedValue({rows:[{id:'c1',firstName:'Ana'}],count:1}),getContact:jest.fn(),createContact:jest.fn().mockResolvedValue({id:'c2'}),updateContact:jest.fn(),deleteContact:jest.fn()}));const app=(await import('../../src/server.js')).default;
describe('contacts API',()=>{test('returns pagination metadata',async()=>{const r=await request(app).get('/api/v1/contacts?page=1');expect(r.status).toBe(200);expect(r.body).toEqual({data:[{id:'c1',firstName:'Ana'}],total:1,page:1});});test('creates a validated contact',async()=>{const r=await request(app).post('/api/v1/contacts').send({firstName:'Ana',email:'ana@abc.example',accountId:'a1'});expect(r.status).toBe(201);expect(r.body.data.id).toBe('c2');});});