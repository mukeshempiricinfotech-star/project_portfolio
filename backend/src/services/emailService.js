const endpoint='https://api.sendgrid.com/v3/mail/send';
export async function sendTicketUpdate({eventId,to,ticketNumber,status}){if(!process.env.SENDGRID_API_KEY)throw new Error('SENDGRID_API_KEY is required');const body={personalizations:[{to:[{email:to}],custom_args:{eventId}}],from:{email:process.env.SENDGRID_FROM_EMAIL},subject:`ABC CRM ticket ${ticketNumber}: ${status}`,content:[{type:'text/plain',value:`Ticket ${ticketNumber} is now ${status}.`}]} ;const res=await fetch(endpoint,{method:'POST',headers:{Authorization:`Bearer ${process.env.SENDGRID_API_KEY}`,'Content-Type':'application/json','Idempotency-Key':eventId},body:JSON.stringify(body)});if(!res.ok)throw new Error(`SendGrid returned ${res.status}`);return res.headers.get('x-message-id');}